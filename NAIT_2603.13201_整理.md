# NAIT：Neuron-Aware Data Selection in Instruction Tuning for Large Language Models — 完整整理

> 論文：**Neuron-Aware Data Selection In Instruction Tuning For Large Language Models**
> arXiv：[2603.13201](https://arxiv.org/abs/2603.13201)（2026 年 3 月）｜ HTML：`arxiv.org/html/2603.13201v1`
> OpenReview：[`uq6UWRgzMr`](https://openreview.net/forum?id=uq6UWRgzMr)
> 方法簡稱：**NAIT**（Neuron-Aware / Neuronal-Activation-based IT data selection）

---

## 0. 來源與可信度說明（請先讀）

本文件是在「**無法直接抓取 PDF/HTML**」的環境下整理而成：本機 egress 政策封鎖了 arXiv / OpenReview / HuggingFace / ResearchGate 等網域（curl 與 WebFetch 都回 403），因此內容是透過多次「伺服器端網頁搜尋（會實際讀取論文頁面再摘要）」交叉比對重建。

因此標示三種可信度：

- ✅ **論文原文／多來源一致**：摘要、方法骨架、評測任務、回報的數字、消融結論。
- 🟡 **論文有提到但細節未能逐字取得**：例如「降維方法」的確切運算子、相似度的確切公式、完整 baseline 表格逐格數字。我會說明到可支撐的程度，並標註不確定處。
- 🛠️ **實作重建（合理推斷）**：第 6/7 節的程式碼為依方法描述忠實重建的可執行骨架，非論文原始碼，已明確標註。

若你之後能提供 PDF 原文，我可以把 🟡 與 🛠️ 的部分校正為逐字精確版本。

---

## 1. 一句話總結（TL;DR）

> **NAIT 把「指令微調資料選擇」變成一個「神經元激活特徵對齊」問題**：先用少量「目標能力的 in-domain 範例」在模型內部錄下神經元激活、壓成一個可重用的「能力激活特徵向量」；再對候選 IT 資料逐筆算激活、與該特徵做相似度（對齊分數）排序，挑分數最高的一小部分（如 10%）來微調。它**不需要外部大模型、不需要梯度/影響函數**，計算便宜，且發現激活特徵具有**跨能力可轉移性**。

核心賣點：**只用 Alpaca-GPT4 的 10% 子集**微調 LLaMA-2-7B，跨 5 項任務平均比「用全量資料微調」**高 +3.24%**，也勝過依賴外部先進模型或不確定性特徵的方法。

---

## 2. 研究背景與動機

### 2.1 問題：指令微調（IT）不是資料越多越好

- 指令微調（Instruction Tuning, IT）是釋放 LLM 能力的有效手段。
- 但近期研究指出：**過量的 IT 資料反而會傷害模型表現**；相對地，**精選一小撮高品質 IT 資料就能顯著提升能力**（「質 > 量」）。
- 於是核心挑戰變成：**如何從大型 IT 資料集中，挑出最有效率的子集**，以發展「特定能力」或「通用能力」。

### 2.2 既有資料選擇方法的痛點（NAIT 想避開的）

| 類別 | 代表方法 | 痛點 |
|---|---|---|
| 依賴外部先進模型評分 | AlpaGasus、DEITA、Nuggets 類 | 需呼叫 GPT-4 等外部模型 → 成本高、受外部模型偏好牽制 |
| 不確定性 / 自我反思 | SelectIT 類 | 以不確定性為特徵，訊號不一定可靠 |
| 困難度 / 困惑度 | IFD、Cherry（perplexity-based） | 只看輸出層難度，未反映模型「內部計算」與目標能力對齊 |
| 梯度 / 影響函數 | LESS（influence + 梯度 + Hessian 近似） | 計算與記憶體成本對大模型而言過於昂貴 |
| 隨機 | Random（"Random is almost all you need"） | 簡單但缺乏能力導向 |

### 2.3 NAIT 的核心洞見

> **模型「內部的神經元激活模式」**能直接反映它對某種能力的「反應偏好」。一筆 IT 資料若能「點亮」與目標能力相同的神經元，就更可能有效培養該能力。

因此 NAIT 改用「**模型自身的神經元激活**」當作資料的表徵（model-centric / 以模型為中心），避開外部模型與昂貴梯度。

---

## 3. 摘要（中譯重點）

> 指令微調（IT）被證實能有效解鎖 LLM 的強大能力。近期研究指出過量 IT 資料會降低表現，而精選一小撮高品質 IT 資料可顯著增強能力。因此「**從 IT 資料集中找出最有效率的子集，以培養特定或通用能力**」成為關鍵挑戰。為此本文提出新穎且高效的框架 **NAIT**。
>
> NAIT 透過分析「**IT 資料**」與「**目標領域能力**」之間**神經元激活模式的相似度**，來評估 IT 資料對 LLM 表現的影響。具體而言，NAIT 從目標能力的 in-domain 資料集中**擷取神經元激活模式，建構可重用且可轉移的「神經元激活特徵」**；再依據候選樣本與目標能力「期望激活特徵」的相似度，評估並挑選最佳樣本。
>
> 實驗顯示：以 NAIT 選出的 **10% Alpaca-GPT4 子集**訓練，在多項任務上**穩定勝過**依賴外部先進模型或不確定性特徵的方法。研究也揭示了神經元激活特徵在 LLM **不同能力間的可轉移性**。

---

## 4. 核心概念（術語表）

| 概念 | 說明 |
|---|---|
| **神經元（Neuron）** | 指 Transformer 中 **FFN（前饋網路）層**的隱藏單元。現代 LLM 多採 **GLU/SwiGLU** 結構：含 up-projection、gate-projection、down-projection 三個矩陣。第 *k* 個神經元對應 **down-proj 的第 *k* 欄**，以及 up-proj / gate-proj 的**第 *k* 列**。 |
| **神經元激活（Activation）** | 模型處理某輸入時，FFN 中各神經元（gate×up 後、經激活函數）的輸出值。代表模型對該輸入的「內部反應」。 |
| **激活特徵（Activation Feature）** | 把「一組 in-domain 範例」的激活**彙整 + 降維**而成的向量，代表模型對「該目標能力」的神經元反應偏好。**可重用、可跨任務轉移**。 |
| **目標領域能力（Target capability）** | 想培養的能力，如：事實知識、數學推理、通用推理、多語理解、程式能力。 |
| **In-domain 參考集** | 少量能代表該能力的範例（用各能力 benchmark 的訓練/開發切分），用來抽取「能力激活特徵」。 |
| **候選資料（Candidate IT data）** | 開放、多樣的大型 IT 池（如 Alpaca-GPT4、Evol-Instruct/WizardLM、Orca-GPT4），要從中選子集。 |
| **特徵對齊分數（Feature Alignment Score）** | 候選樣本的激活特徵 vs. 目標能力激活特徵的相似度。分數高 → 該樣本更能培養目標能力。 |
| **可轉移性（Transferability）** | 用 A 能力抽出的激活特徵去選資料，往往也能提升 B、C 等其他能力。 |

---

## 5. 方法總覽（NAIT 兩階段）

```mermaid
flowchart TD
    subgraph S1[階段一：抽取「能力激活特徵」]
      A[少量 in-domain 範例\n（代表目標能力）] --> B[前向傳播 LLM\n錄取 FFN 神經元激活\n（取倒數第二層 penultimate）]
      B --> C[跨 token / 跨樣本彙整\n＋ 降維]
      C --> D[(可重用的\n能力激活特徵 f_cap)]
    end

    subgraph S2[階段二：能力驅動的資料選擇]
      E[候選 IT 池\n（Alpaca-GPT4 等）] --> F[逐筆前向傳播\n取同層神經元激活 → f_i]
      D --> G{對齊分數\nsim(f_i, f_cap)}
      F --> G
      G --> H[依分數排序\n取 Top-k%（如 10%）]
    end

    H --> I[在選中的子集上做\n指令微調 Instruction Tuning]
    I --> J[在對應能力測試集評測]
```

### 階段一：抽取能力激活特徵（Activation Feature Extraction）

1. 準備**少量** in-domain 範例（消融證明「少量即可」，見 §9）。
2. 把這些範例餵入 LLM 前向傳播，**錄下 FFN 神經元激活狀態**。
   - ✅ 取**倒數第二層（penultimate layer）**的激活（層選擇消融的結論，見 §9.2）。
3. 透過**降維方法**把激活彙整成一個固定維度的**能力激活特徵向量** `f_cap`。
   - 🟡 論文描述為「construct an activation feature through a dimensionality reduction method」。確切運算子（平均池化 / 取激活頻率 / PCA 等）未能從可取得的摘要逐字確認；§7 以「跨 token 平均 + 跨樣本平均」做忠實重建。
4. 此特徵被描述為「對目標任務的**回應與神經激活偏好**之表徵」，且**可重用、可跨能力轉移**。

### 階段二：能力驅動的資料選擇（Capability-Driven Selection）

1. 對候選 IT 池中**每一筆樣本**做同樣的前向傳播，取同層激活 → 得到該樣本激活特徵 `f_i`。
2. 計算**對齊分數** `score_i = sim(f_i, f_cap)`（候選樣本與能力特徵的相似度）。
   - 🟡 相似度度量的確切形式（cosine / 內積 / 其他）未能逐字確認；§7 以 cosine 重建。
3. 依分數**由高到低排序**，取 **Top-k%**（主結果用 **10%**）。
4. 在選中的子集上做標準指令微調，於對應能力測試集評測。

> ✅ 關鍵驗證：消融顯示「**取對齊分數最高**」的子集，比 Random 高 **+3.35%**；代表「高對齊分數 → 真的更能提升能力」，分數方向有效（見 §9.3）。

---

## 6. 演算法（虛擬碼）

```text
輸入：
  M           : 已預訓練 / 基礎 LLM
  D_indomain  : 目標能力的少量參考範例
  D_cand      : 候選 IT 資料池
  L           : 取激活的層（= 倒數第二層 penultimate）
  k           : 選擇比例（如 0.10）

# 階段一：能力激活特徵
A = []
for x in D_indomain:
    h = forward_collect_ffn_activations(M, x, layer=L)   # shape: [num_tokens, num_neurons]
    a_x = pool_over_tokens(h)                             # → [num_neurons]
    A.append(a_x)
f_cap = reduce(A)                                          # 跨樣本彙整 + 降維 → 能力激活特徵

# 階段二：對齊分數 + 選擇
scores = []
for i, s in enumerate(D_cand):
    h = forward_collect_ffn_activations(M, s, layer=L)
    f_i = pool_over_tokens(h)
    scores.append( similarity(f_i, f_cap) )               # 對齊分數（高 = 越貼近目標能力）

idx = argsort_desc(scores)[: int(k * len(D_cand)) ]       # Top-k%
D_selected = D_cand[idx]

# 微調
M_ft = instruction_tune(M, D_selected)
return M_ft
```

---

## 7. 如何實作（PyTorch + HuggingFace 重建骨架）🛠️

> 下列程式為**依方法描述忠實重建**的可執行骨架（非論文原始碼）。用 forward hook 抓 SwiGLU 的神經元激活，取倒數第二層，跨 token 平均得樣本特徵，跨 in-domain 樣本平均得能力特徵，再用 cosine 當對齊分數選 Top-k。

```python
import torch, torch.nn.functional as F
from transformers import AutoModelForCausalLM, AutoTokenizer

model_name = "meta-llama/Llama-2-7b-hf"
tok = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(model_name, torch_dtype=torch.bfloat16,
                                             device_map="auto", output_hidden_states=False)
model.eval()

# ---- 1) 用 forward hook 取「FFN 神經元激活」 ----
# LLaMA: layer.mlp 的輸入 = act_fn(gate_proj(x)) * up_proj(x) 再進 down_proj。
# 「神經元激活」= down_proj 的輸入（即 SwiGLU 中介維度，每個神經元一格）。
PENULTIMATE = model.config.num_hidden_layers - 2   # 倒數第二層
_act = {}

def hook(module, inp, out):
    # inp[0]: [batch, seq, intermediate_size] = 進入 down_proj 前的逐神經元激活
    _act["a"] = inp[0].detach().float()

h = model.model.layers[PENULTIMATE].mlp.down_proj.register_forward_hook(hook)

@torch.no_grad()
def sample_feature(text: str) -> torch.Tensor:
    ids = tok(text, return_tensors="pt", truncation=True, max_length=2048).to(model.device)
    model(**ids)
    a = _act["a"][0]                 # [seq, num_neurons]
    return a.mean(dim=0)             # 跨 token 平均 → [num_neurons]（🛠️ pooling 為重建）

# ---- 2) 由少量 in-domain 範例建「能力激活特徵」 f_cap ----
def capability_feature(indomain_texts) -> torch.Tensor:
    feats = torch.stack([sample_feature(t) for t in indomain_texts])  # [N, num_neurons]
    return feats.mean(dim=0)         # 跨樣本平均 + 降維（🛠️ 重建）

# ---- 3) 對候選池逐筆算對齊分數，選 Top-k ----
@torch.no_grad()
def select(cand_texts, f_cap, k=0.10):
    scores = []
    for t in cand_texts:
        f_i = sample_feature(t)
        scores.append(F.cosine_similarity(f_i, f_cap, dim=0).item())  # 對齊分數（🛠️ cosine 為重建）
    scores = torch.tensor(scores)
    n = int(k * len(cand_texts))
    top = torch.topk(scores, n).indices.tolist()
    return [cand_texts[i] for i in top], scores

# ---- 用法 ----
# f_cap = capability_feature(mmlu_fewshot_examples)        # 例：用 MMLU 當能力參考
# subset, scores = select(alpaca_gpt4_texts, f_cap, k=0.10) # 選 10%
# 之後對 subset 做標準 SFT / instruction tuning
h.remove()
```

實作要點與對照論文：
- **取激活的位置**：SwiGLU 中 `down_proj` 的輸入即「逐神經元激活」（gate 經激活函數後 × up）。論文明言第 *k* 神經元對應 down-proj 第 *k* 欄、up/gate-proj 第 *k* 列。✅
- **取哪一層**：倒數第二層（penultimate）。✅（層消融結論）
- **池化 / 降維 / 相似度**：🛠️ 上面用「token 平均 → 樣本平均 → cosine」為合理重建；論文用語為「dimensionality reduction」+「similarity / alignment score」，確切運算子待原文校正。
- **效率**：整套只需**前向傳播**，**無外部模型、無反向傳播/梯度/Hessian**，可批次化、可快取候選特徵。✅

---

## 8. 實驗設定

### 8.1 基礎模型（展示泛用性，跨 5 個模型）✅
- LLaMA-2-7B、LLaMA-2-13B、LLaMA-3-8B、Mistral-7B、Qwen-2.5-7B

### 8.2 候選 IT 資料集 ✅
- **Alpaca-GPT4**（主）、**Evol-Instruct / WizardLM**、**Orca-GPT4**

### 8.3 目標能力 / In-domain 參考 + 評測任務（5 項）✅

| 能力 | 評測 Benchmark |
|---|---|
| 事實知識（Factual knowledge） | **MMLU** |
| 數學推理（Math reasoning） | **GSM（GSM8K）** |
| 通用推理（General reasoning） | **BBH**（Big-Bench Hard） |
| 多語理解（Multilingual） | **TyDiQA** |
| 程式能力（Coding） | **HumanEval**（CodeX 系列） |

> 以各能力的 **in-domain 切分**作為能力參考來抽特徵，並於對應**測試集**評測。

### 8.4 對照 baselines ✅
- 隨機：**Random**（隨機 10% 子集）
- 表面特徵：**Length**、**Perplexity**
- 困難度：**IFD / Cherry**
- 外部模型評分：**DEITA**、**Nuggets**、**AlpaGasus** 類
- 不確定性：**SelectIT** 類
- 梯度/影響函數：**LESS** 類

主結果表（論文 Table 2）以**粗體=最佳、底線=次佳**標示。

---

## 9. 主要結果（論文回報）✅🟡

> 數字為論文回報、經搜尋取得；完整逐格 baseline 表未能逐字抓取，故以「可確認的關鍵數字 + 質性結論」呈現。

### 9.1 核心結果（LLaMA-2-7B / Alpaca-GPT4，選 10%）
- 對比「**用全量資料做 IT**」：跨 5 項任務**平均 +3.24%**。✅
- 對比「**Random 10%**」：取**最高對齊分數**子集 **+3.35%**。✅
- **穩定勝過**依賴外部先進模型或不確定性特徵的方法。✅

### 9.2 各能力以對應特徵選資料的提升（同設定下的增益）🟡
| 能力特徵 | 回報增益 |
|---|---|
| MMLU | +3.20% |
| GSM | +4.65% |
| HumanEval / Code | +2.88% |
| TyDiQA | +1.89% |
| BBH | +1.23% |

### 9.3 跨基礎模型（NAIT 子集 vs. 基線）🟡
- **LLaMA-2-13B**：較 Alpaca 基線 **+7.02%**
- **Mistral-7B**：**+21.92%**（增益最大，顯示對不同骨幹皆有效）

### 9.4 跨 IT 資料集的「最佳比例」不同 ✅
- **Alpaca-GPT4**：所需比例最低（約 **10%** 即達最佳）
- **Orca-GPT4**：約 **50%** 達最佳
- **Evol-Instruct / WizardLM**：約 **80%** 達最佳
- 洞見：**資料集越「冗餘 / 同質」，可砍越多**；不同資料集的最佳保留比例不同。

---

## 10. 消融研究與洞見（重點）✅

> 這節是論文最有價值的部分之一：每個消融都對應一條可操作的洞見。

### 10.1 In-domain 樣本數量：少量即足
- **發現**：只需**少量** in-domain 範例，就能準確抽出能反映模型能力的激活特徵。
- **洞見**：能力激活特徵是**穩定、低樣本需求**的；建構成本極低，且一旦建好可**重用**於整個候選池。

### 10.2 取哪一層：倒數第二層（penultimate）最佳
- **發現**：**淺層**激活 → 結果**不穩定 / 次佳**；**深層、尤其倒數第二層** → 表現強。所有實驗皆採倒數第二層。
- **洞見**：高層激活更貼近「語意 / 能力」層級的表徵，最適合作為能力對齊訊號；淺層偏低階特徵，鑑別力不足。

### 10.3 對齊分數的方向性：高分有效
- **發現**：選**最高對齊分數**子集 > Random（**+3.35%**）。
- **洞見**：對齊分數**確實與能力增益正相關**——分數是有意義的選擇訊號，而非雜訊；反向（選低分）會更差，佐證因果方向。

### 10.4 選擇比例：存在「夠用即止」的甜蜜點
- **發現**：最佳保留比例隨資料集而異（Alpaca-GPT4≈10%、Orca-GPT4≈50%、Evol-Instruct≈80%）；超過甜蜜點再加資料無益甚至有害。
- **洞見**：呼應「質 > 量」——**並非越多越好**；NAIT 能找出各資料集的有效核心比例。

### 10.5 可轉移性：跨能力遷移
- **發現**：用某能力（如 **MMLU**）抽的激活特徵去選資料，也能**顯著提升其他任務**。
- **洞見**：神經元激活特徵捕捉到**跨能力共享的底層計算**，不是只服務單一 benchmark；能力之間存在可遷移結構。

### 10.6 哪種資料最通用：邏輯推理 / 程式特徵
- **發現**：**帶有更多邏輯推理與程式（programmatic）特徵的 IT 資料，具有最強的通用可轉移性**，能跨多任務同時增強能力。
- **洞見**：若目標是「通用能力」，**優先挑富含推理/程式結構的樣本**最划算；這類資料是能力的「通用放大器」。

### 10.7 穩定核心子集（Stable Core Subset）
- **發現**：存在一個**穩定的核心資料子集**，能持續**點亮模型的基礎能力**，並普遍提升各任務表現。
- **洞見**：模型的基礎能力由**一小撮反覆被激活的核心資料**支撐；找到並保留它即可廣泛受益。

### 10.8 效率：免外部模型、免梯度
- **發現**：NAIT 只靠**少量 in-domain 範例 + 前向傳播**；**不需外部先進模型、不需梯度/影響函數**（對照 LESS 的梯度+Hessian 近似昂貴）。
- **洞見**：在**準確度與計算成本**之間取得很好的平衡，對大模型可規模化；候選激活可預先快取，重複選擇近乎免費。

---

## 11. 與相關工作的關係

| 相關工作 | 與 NAIT 的關係 |
|---|---|
| **MoNA**（[arXiv 2503.15573](https://arxiv.org/abs/2503.15573)，NeurIPS 2025，"Task-Specific Data Selection ... via **Monosemantic Neuronal Activations**"） | 同一脈絡的「以神經元激活當樣本表徵」工作。MoNA 用 **稀疏自編碼器（SAE）** 把多義（polysemantic）激活解耦成**單義（monosemantic）**表徵並設計專屬相似度，解決「原始激活造成虛假相似」；NAIT 走「**倒數第二層原始激活 + 降維 + 對齊分數**」的輕量路線，並強調**能力可轉移性**。可視為姊妹/延伸工作。 |
| **LESS**（梯度/影響函數） | NAIT 的對照組；NAIT 以**免梯度**的激活特徵取代昂貴的梯度+Hessian。 |
| **DEITA / AlpaGasus / Nuggets** | 依賴**外部先進模型**評分；NAIT 不需外部模型。 |
| **IFD / Cherry** | 以困惑度/困難度為訊號；NAIT 改用**內部激活對齊**。 |
| **SelectIT** | 以**不確定性/自我反思**為訊號；NAIT 以激活特徵取代。 |
| **Random（"Random is almost all you need"）** | 強簡單基線；NAIT 一致勝過 Random（+3.35%）。 |

---

## 12. 貢獻、優點、限制與適用情境

### 12.1 主要貢獻 ✅
1. **首個**以「**神經元激活模式**」為基礎的 IT 資料選擇框架，為「**能力導向**」的模型培養提出新範式。
2. 揭示「**激活特徵 ↔ 基礎能力保留**」的關聯，並系統性探討其**跨任務可轉移性**。
3. 提出**可重用、可轉移**的能力激活特徵，**免外部模型、免梯度**，高效。

### 12.2 優點
- 💰 **便宜**：只需前向傳播；候選特徵可快取重用。
- 🔁 **可重用/可轉移**：一次抽特徵，跨能力可用。
- 🎯 **能力導向**：可針對「特定能力」或「通用能力」客製選資料。
- 🧩 **泛用**：跨多個基礎模型、IT 資料集、能力域皆有效。

### 12.3 限制 / 注意事項 🟡
- 需要對候選池**逐筆前向傳播**抽激活（雖便宜，但池極大時仍有成本；可快取緩解）。
- 需要**少量 in-domain 參考**來定義目標能力（雖少，但仍需取得）。
- 🟡 降維/相似度的**確切運算子**與**完整 baseline 表**未能在本環境逐字取得，建議對照原文。
- 激活特徵與「倒數第二層」綁定，跨架構（非 SwiGLU/GLU）需調整神經元定義。

### 12.4 什麼時候用 NAIT
- 你有**大型且冗餘**的 IT 資料池，想**砍成本**又**保住/提升**能力。
- 你想**針對特定能力**（數學/程式/多語…）做資料選擇。
- 你**不想**或**不能**依賴外部大模型評分或昂貴的梯度方法。

---

## 13. 重點速記（Cheat Sheet）

- **本質**：資料選擇 → 神經元激活特徵**對齊**問題。
- **三步**：①少量 in-domain 抽「能力激活特徵」（倒數第二層）→ ②候選逐筆算「對齊分數」→ ③選 Top-k% 微調。
- **不需**：外部大模型、梯度、Hessian。
- **數字**：LLaMA-2-7B + 10% Alpaca-GPT4 → 跨 5 任務平均 **+3.24%**（vs 全量）、**+3.35%**（vs Random）。
- **層**：倒數第二層最佳（淺層不穩）。
- **比例甜蜜點**：Alpaca-GPT4≈10%、Orca-GPT4≈50%、Evol-Instruct≈80%。
- **最通用的資料**：富含**邏輯推理 / 程式**特徵者。
- **大發現**：激活特徵**跨能力可轉移**；存在**穩定核心子集**支撐基礎能力。

---

## 14. 參考連結

- 論文（abs）：https://arxiv.org/abs/2603.13201
- 論文（HTML）：https://arxiv.org/html/2603.13201v1
- 論文（PDF）：https://arxiv.org/pdf/2603.13201
- OpenReview：https://openreview.net/forum?id=uq6UWRgzMr
- 相關工作 MoNA：https://arxiv.org/abs/2503.15573 ／ https://openreview.net/forum?id=9AHkbALT2t
- 對照方法：LESS、DEITA、IFD/Cherry（[GitHub](https://github.com/tianyi-lab/Cherry_LLM)）、SelectIT（[arXiv 2402.16705](https://arxiv.org/abs/2402.16705)）、AlpaGasus（[arXiv 2307.08701](https://arxiv.org/abs/2307.08701)）、"Random is Almost All You Need"（[arXiv 2410.09335](https://arxiv.org/abs/2410.09335)）

---

*整理者註：因執行環境封鎖 arXiv/OpenReview 直連，本文件由伺服器端網頁搜尋交叉比對重建。標 ✅ 者為多來源一致；標 🟡 者為論文有述但細節未能逐字取得；標 🛠️ 者為依方法描述之實作重建。如需逐字精確版（公式、完整表格），請提供 PDF 原文，我可據此校正。*
