# NAIT：Neuron-Aware Data Selection in Instruction Tuning for Large Language Models — 完整整理

> **論文**：Neuron-Aware Data Selection In Instruction Tuning For Large Language Models
> **發表**：**ICLR 2026**（conference paper）｜ arXiv：[2603.13201v1](https://arxiv.org/abs/2603.13201)（2026-03-13）｜ OpenReview：[`uq6UWRgzMr`](https://openreview.net/forum?id=uq6UWRgzMr)
> **作者**：Xin Chen, Junchao Wu（共同一作）, Shu Yang, Runzhe Zhan, Zeyu Wu, Min Yang†, Shujian Huang, Lidia S. Chao, Derek F. Wong†（†通訊）
> **單位**：澳門大學 NLP²CT Lab、深圳先進技術大學/中科院 SIAT、KAUST、南京大學
> **開源**：跨任務神經元特徵庫（cross-task neuron feature library）＋ **Alpaca-NAIT** 資料集（接收後釋出）
> **方法簡稱**：**NAIT**（Neuronal Activation-based efficient IT data selection framework）

> 本文件依據 v1 PDF 原文逐節整理，方法公式、演算法、表格數字均取自原文。

---

## 1. 一句話總結（TL;DR）

> **NAIT 把「指令微調資料選擇」變成「神經元激活方向對齊」問題**：
> 用少量「目標能力 in-domain 範例」在模型每一層錄下激活，取「**最後 token − 第一個 token**」的激活變化、對它做 **PCA 取第一主成分** 當作該能力在該層的「**方向向量** `vₗ`」（並校正方向正負號）；再把每筆候選 IT 資料的激活**投影到這些方向、跨層加總**得到分數 `sy`，挑分數最高的 **top-k**（如 10%）來微調。
> 全程**只需前向傳播**，**不用外部大模型、不用梯度/Hessian**，成本極低（52k 資料只要 **1.32 小時 / $1.52**），且發現激活方向具有**跨能力可轉移性**。

核心結果：LLaMA-2-7B 只用 **10% Alpaca-GPT4**（NAIT 選），跨 9 個測試平均比「用 **100%** 資料做全量微調」**高 +3.24%**（單看數學能力特徵可達 **+4.65%**），並勝過 AlpaGasus、Q2Q、SelectIT 等方法。

---

## 2. 研究背景與動機

### 2.1 問題：IT 不是資料越多越好
- 指令微調（Instruction Tuning, IT）是釋放 LLM 能力的基礎技術（Ouyang et al., 2022）。
- **過量 IT 資料反而傷害表現**；**精選少量高品質資料**可顯著提升（如 **LIMA** 只用 1k 資料就有亮眼結果）——「**質 > 量**」。
- 核心挑戰：如何從大型 IT 資料集中，挑出**最有效率的子集**來培養「特定」或「通用」能力，且要**可解釋**、能**針對特定目標能力**。

### 2.2 既有方法四大類與痛點（原文 §2.2）
| 類別 | 代表 | 痛點 |
|---|---|---|
| ① 手工特徵（Handcrafted） | DQI、n-gram | 只看表面詞彙特徵，抓不到深層性質 |
| ② 模型輸出特徵（uncertainty/perplexity） | Instruction Mining、SelectIT | 依賴模型輸出、可能有偏、黑箱、可解釋性差 |
| ③ LLM 當評分器（LLM-as-Scorer） | InsTag、AlpaGasus（ChatGPT 評分） | 貴、不透明、依賴閉源 API、難規模化 |
| ④ Loss/梯度 coreset | Q2Q、**LESS**（梯度+Hessian 近似） | 計算量大、記憶體吃重、有近似誤差，且易過擬合目標 |

### 2.3 NAIT 的核心假設（原文 §3.1 Motivation）
> 神經科學/可解釋性研究顯示：**特定任務只會激活特定的神經元子集**。
> **核心假設**：當 LLM 處理一筆樣本時，**其神經元激活模式越貼近「目標能力」的激活特徵，這筆樣本就越能提升模型在該領域的表現**。

→ 因此改用「**模型自身的神經元激活**」當作資料表徵（model-centric），避開外部模型與昂貴梯度。

### 2.4 NAIT vs 既有方法（原文 Table 1）
| 方法 | 特徵來源 | 不依賴外部 | 免梯度 | 可針對能力 | 低成本 | 可解釋 |
|---|---|:--:|:--:|:--:|:--:|:--:|
| LIMA | 人工 | ✓ | ✓ | ✗ | ✗ | ✗ |
| Instruction Mining | PPL & Reward… | ✓ | ✓ | ✗ | ✗ | ◐ |
| AlpaGasus | ChatGPT 評分 | ✗ | ✓ | ✗ | ✗ | ✗ |
| SelectIT | 多粒度不確定性 | ✓ | ✓ | ✗ | ✗ | ◐ |
| LESS | 梯度 | ✓ | ✗ | ✓ | ✗ | ◐ |
| **NAIT（本文）** | **神經元激活** | **✓** | **✓** | **✓** | **✓** | **✓** |

---

## 3. 摘要（中譯）

> 指令微調（IT）能有效解鎖 LLM 的強大能力。近期研究指出過量 IT 資料會降低表現，而精選一小撮高品質 IT 資料可顯著增強能力。因此「從 IT 資料集中找出最有效率的子集，以培養特定或通用能力」成為關鍵挑戰。為此本文提出新穎且高效的框架 **NAIT**。NAIT 透過分析「IT 資料」與「目標領域能力」之間**神經元激活模式的相似度**，評估 IT 資料對表現的影響：先從目標能力的 in-domain 資料集**擷取可重用、可轉移的神經元激活特徵**，再依候選樣本與目標能力「期望激活特徵」的相似度挑選最佳樣本。實驗顯示，以 NAIT 選出的 **10% Alpaca-GPT4 子集**訓練，在多項任務上穩定勝過依賴外部先進模型或不確定性特徵的方法。研究亦揭示神經元激活特徵在不同能力間的**可轉移性**：**富含邏輯推理與程式特徵**的 IT 資料具有強通用可轉移性；且存在一個**穩定核心子集**可持續活化模型基礎能力、普遍提升各任務表現。

---

## 4. 核心概念（術語表）

| 概念 | 說明 |
|---|---|
| **神經元激活 `A⁽ˡ⁾(tₖ)`** | 對 in-domain/候選樣本前向傳播時，第 `l` 層、第 `k` 個 token 的激活向量 `[a₁,…,a_J]`，`J` = 該層神經元數。 |
| **激活變化 `ΔA⁽ˡ⁾`** | **最後 token − 第一個 token** 的激活差：`ΔA⁽ˡ⁾ = A⁽ˡ⁾(t_K) − A⁽ˡ⁾(t₁)`，量化整段生成過程中每個神經元的「動態位移」。 |
| **方向向量 `vₗ`（能力激活特徵）** | 對一組 in-domain 樣本的 `ΔA⁽ˡ⁾` 做 **PCA 取第一主成分**，得到該能力在第 `l` 層的特徵方向；再用平均差 `μ_diff` 校正正負號。所有層的集合 `V={vₗ}` 即「可重用、可轉移」的能力激活特徵。 |
| **對齊分數 `sy`** | 候選樣本激活在各層投影到 `vₗ` 後跨層加總：`sy = Σₗ (A⁽ˡ⁾ · vₗ)`。分數越高 → 越能活化目標能力相關神經元。 |
| **In-domain 參考集 `P`** | 少量代表某能力的範例（取各 benchmark 的 in-domain split），用來抽方向向量。 |
| **候選 IT 池 `D_ins`** | 開放多樣的大型 IT 資料（Alpaca-GPT4 / Evol-Instruct / Orca-GPT4），待選子集。 |
| **可轉移性 `Transferability_i`** | 用能力 `i` 的特徵去選資料時，對「其他能力 `j`」帶來的增益（公式見 §10.6）。 |

---

## 5. 方法總覽（NAIT 兩大模組）

```mermaid
flowchart TD
    subgraph A[模組(A)：目標能力的神經元激活特徵抽取]
      a1[少量 in-domain 範例 P\n（代表目標能力 C）] --> a2[逐層、逐樣本前向傳播\n取每層 第一個token 與 最後token 的激活]
      a2 --> a3["ΔA⁽ˡ⁾ = A⁽ˡ⁾(t_K) − A⁽ˡ⁾(t₁)\n（last − first token 激活位移）"]
      a3 --> a4["每層做 PCA 取第一主成分\nvₗ = PCA(ΔA⁽ˡ⁾)"]
      a4 --> a5["以平均差 μ_diff 校正方向正負號\n若 μ_diff·vₗ<0 則 vₗ ← −vₗ"]
      a5 --> a6[(方向向量集 V = {vₗ}\n= 可重用能力激活特徵)]
    end

    subgraph B[模組(B)：激活方向引導的資料選擇]
      b1[候選 IT 池 D_ins] --> b2[逐筆前向傳播\n取各層激活 A⁽ˡ⁾]
      a6 --> b3["對齊分數\nsy = Σₗ (A⁽ˡ⁾ · vₗ)"]
      b2 --> b3
      b3 --> b4["排序取 top-k\nD_selected = top-k(S)"]
    end

    b4 --> c1[在選中的子集上做\n標準（全量）指令微調]
    c1 --> c2[在對應能力測試集評測]
```

**重點澄清（避免常見誤解）**：
- 特徵**不是**「把激活平均成一個向量再算 cosine」。而是 **PCA 主成分方向 + 投影加總**。
- **使用「所有解碼層 `L`」**（每層各一個方向向量、分數跨層加總），**並非只取某一層**。
- 用的是**「last − first token」的激活位移**（動態變化），不是單一 token 的絕對激活。
- 微調採**全量微調（full fine-tuning）**，非 LoRA（LoRA 僅用於 LESS baseline 的暖身）。

---

## 6. 演算法（原文 Algorithm 1，逐字結構）

```text
Input : 目標能力 in-domain 資料 P；指令資料 D_ins；模型 M；激活層 L；選擇預算 top-k
Output: 選中的子集 D_selected

# Stage 1：目標能力激活擷取
for each l in L: ΔA⁽ˡ⁾ ← ∅
for each in-domain sample P_i = (t1,...,tK) in P:
    for each layer l in L:
        從 M 取 A⁽ˡ⁾(t1) 與 A⁽ˡ⁾(tK)
        ΔA⁽ˡ⁾_i ← A⁽ˡ⁾(tK) − A⁽ˡ⁾(t1)        # 第一個→最後 token 的激活變化
        ΔA⁽ˡ⁾ ← ΔA⁽ˡ⁾ ∪ {ΔA⁽ˡ⁾_i}

# Stage 2：以 PCA 抽方向
V ← ∅
for each layer l = 1..L:
    vₗ ← PCA(ΔA⁽ˡ⁾)                            # 取第一主成分
    μ_diff ← (1/|P|) Σ_i (A⁽ˡ⁾(tK) − A⁽ˡ⁾(t1))
    if μ_diff · vₗ < 0:  vₗ ← −vₗ              # 對齊方向正負號
    V ← V ∪ {vₗ}

# Stage 3：激活方向引導的評分
S ← ∅
for each sample y in D_ins:
    用 M(y) 取各層激活 A⁽ˡ⁾                      # 以整段序列的 token 平均彙整
    sy ← Σ_{l=1..L} (A⁽ˡ⁾ · vₗ)                # 投影到目標方向、跨層加總
    S ← S ∪ {sy}

# Stage 4：排序與選擇
D_selected ← top-k(S)
return D_selected
```

對應公式：`A(tk)=[a_j]_{j=1..J}` (1)；`ΔA⁽ˡ⁾=A⁽ˡ⁾(t_K)−A⁽ˡ⁾(t₁)` (2)；`vₗ=PCA(ΔA⁽ˡ⁾)` (3)；正負號校正 (4)；`sy=Σₗ(A⁽ˡ⁾·vₗ)` (5)；`D_selected=top-k(S)` (6)。

---

## 7. 如何實作（PyTorch + HuggingFace，依 Algorithm 1 重建）🛠️

> 下列為依演算法忠實重建的可執行骨架（非論文原始碼）。以 `output_hidden_states=True` 取「各解碼層」激活，計算 last−first token 位移 → 每層 PCA 取第一主成分（含正負號校正）→ 候選逐筆投影加總 → 取 top-k。

```python
import torch, numpy as np
from sklearn.decomposition import PCA
from transformers import AutoModelForCausalLM, AutoTokenizer

name = "meta-llama/Llama-2-7b-hf"
tok = AutoTokenizer.from_pretrained(name)
model = AutoModelForCausalLM.from_pretrained(
    name, torch_dtype=torch.bfloat16, device_map="auto", output_hidden_states=True).eval()

@torch.no_grad()
def layer_acts(text):
    ids = tok(text, return_tensors="pt", truncation=True, max_length=2048).to(model.device)
    hs = model(**ids).hidden_states          # tuple: (emb, layer1, ..., layerL)
    layers = hs[1:]                          # 各解碼層輸出，每個 [1, seq, hidden]
    first = torch.stack([h[0, 0]  for h in layers])   # [L, hidden]  第一個 token
    last  = torch.stack([h[0, -1] for h in layers])   # [L, hidden]  最後 token
    mean  = torch.stack([h[0].mean(0) for h in layers])  # [L, hidden] 全序列平均（供評分）
    return first.float().cpu(), last.float().cpu(), mean.float().cpu()

# ---- 模組(A)：由 in-domain 範例抽「每層方向向量 vₗ」 ----
def extract_directions(indomain_texts):
    F, La = zip(*[layer_acts(t)[:2] for t in indomain_texts])   # 收集 first / last
    F, La = torch.stack(F), torch.stack(La)                     # [N, L, hidden]
    dA = (La - F)                                               # ΔA：last − first  [N, L, hidden]
    L = dA.shape[1]
    V = []
    for l in range(L):
        X = dA[:, l, :].numpy()                                 # [N, hidden]
        v = torch.tensor(PCA(n_components=1).fit(X).components_[0])  # 第一主成分
        mu = torch.tensor(X.mean(0))                            # μ_diff
        if torch.dot(mu, v) < 0: v = -v                         # 正負號校正 (Eq.4)
        V.append(v / v.norm())
    return torch.stack(V)                                       # [L, hidden]

# ---- 模組(B)：對候選池逐筆評分、取 top-k ----
@torch.no_grad()
def select(cand_texts, V, k=0.10):
    scores = []
    for t in cand_texts:
        A = layer_acts(t)[2]                                    # [L, hidden] 全序列平均激活
        scores.append(float((A * V).sum()))                    # sy = Σₗ A⁽ˡ⁾·vₗ  (Eq.5)
    scores = torch.tensor(scores)
    n = int(k * len(cand_texts))
    top = torch.topk(scores, n).indices.tolist()               # top-k (Eq.6)
    return [cand_texts[i] for i in top], scores

# ---- 用法 ----
# V = extract_directions(mmlu_indomain_texts)          # 例：用 MMLU in-domain 抽方向
# subset, _ = select(alpaca_gpt4_texts, V, k=0.10)     # 選 10%（最佳掃描為 top-30%）
# 之後對 subset 做標準全量 SFT
```

**實作要點與原文對照**
- **激活取哪裡**：原文寫「activations across the decoder layers」、`J=該層神經元數`。上面以**各解碼層 hidden state（殘差流）**實作為最直接版本；若要嚴格對齊「FFN 神經元」可改 hook `mlp` 的中介激活（SwiGLU：`gate`經激活後 ×`up`，即 `down_proj` 的輸入）。🛠️
- **方向**：每層用 `ΔA=last−first` 做 PCA 第一主成分 + 正負號校正。✅
- **評分**：候選樣本「全序列平均」激活，跨層投影加總（Stage 3）。✅
- **效率**：純前向傳播、可批次、候選激活可快取重用；**無外部模型、無反向傳播**。✅

---

## 8. 實驗設定（原文 §4.1 與 Appendix D）

### 8.1 基礎模型
- 主實驗：**LLaMA-2-7B**（全量微調）。
- 泛化驗證：**LLaMA-2-13B**（大尺度）、**Mistral-7B**、**LLaMA-3-8B**（不同架構）、**Qwen-2.5-7B**（更強基線）。

### 8.2 候選 IT 資料集（大小）
- **Alpaca-GPT4**（52,002，主）、**Evol-Instruct / WizardLM**（70,000）、**Orca-GPT4**（100,000）。

### 8.3 五大能力 / in-domain 參考（大小）＋ 評測（含 OOD 泛化集）
| 能力 | In-domain（活化用） | 主測試 | OOD 泛化測試 | 設定 |
|---|---|---|---|---|
| 事實知識 | MMLU（1,531） | MMLU | MMLU-Pro | 5-shot |
| 數學推理 | GSM（7,473） | GSM | SVAMP | 5-shot CoT |
| 程式能力 | **BigCodeBench**（1,140） | **HumanEval（pass@10, T=0.8）** | MBPP | — |
| 多語理解 | TyDiQA（49,400） | TyDiQA | XQuAD | 5-shot, gold-passage |
| 通用推理 | BBH（4,556） | BBH | — | 5-shot CoT |

> 表格中「CodeX」= 以 BigCodeBench 抽的程式能力特徵；「H-Eval」= HumanEval。

### 8.4 對照 baselines
- IT 建構/選擇：**Alpaca-GPT4（全量）**、**LIMA**、**AlpaGasus**（ChatGPT 評分）、**Q2Q**（前驅模型 loss）、**SelectIT**（不確定性）、**Random（10%）**。
- 能力導向法（Appendix E）：embedding-based、representation-based、**LESS**（梯度）。

### 8.5 微調超參數
- **全量微調**，3 epochs，batch size 128，Adam(β₁=0.9, β₂=0.999)，lr=2e-5（cosine 衰減至 0），序列長 2048，**4×A800**。
- 消融：1 epoch、batch 32（13B）/64（8B）。生成用 greedy（HumanEval：T=0.8, top-p=0.95）。

---

## 9. 主要結果（原文 Table 2，LLaMA-2-7B，皆用 10% 資料；∆ 相對「Alpaca-GPT4 全量」）

| ID | 方法 | MMLU | MMLU-Pro | GSM | SVAMP | H-Eval | MBPP | TyDiQA | XQuAD | BBH | **AVG** | **∆** |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 01 | Alpaca-GPT4（全量 100%） | 46.87 | 21.89 | 14.63 | 39.00 | 27.87 | 51.58 | 39.48 | 42.99 | 39.94 | 36.03 | — |
| 02 | LIMA | 45.20 | 23.04 | 15.76 | 37.67 | 27.75 | 46.56 | 44.92 | 44.72 | 39.91 | 36.17 | +0.39% |
| 03 | +AlpaGasus | 43.21 | 21.96 | 13.34 | 36.67 | 23.94 | 46.08 | 44.70 | 46.84 | 39.91 | 35.18 | −2.34% |
| 04 | +Q2Q | 46.73 | 21.50 | 14.50 | 35.00 | 25.19 | 44.97 | 44.41 | 48.44 | 40.34 | 35.68 | −0.98% |
| 05 | +SelectIT | 47.90 | 22.86 | 15.40 | 41.11 | 27.92 | 49.47 | 43.91 | 45.56 | 40.33 | 37.16 | +3.15% |
| 06 | +Random | 47.14 | 21.43 | 14.13 | 35.67 | 25.55 | 47.35 | 44.16 | 46.56 | 39.21 | 35.69 | −0.94% |
| 07 | +NAIT(MMLU) | 47.81 | 23.61 | 15.68 | 39.67 | 25.23 | 47.47 | 47.16 | 49.47 | 38.52 | 37.18 | +3.20% |
| 08 | **+NAIT(GSM)** | 46.45 | 24.50 | 16.00 | 41.33 | 27.84 | 48.41 | 46.54 | 47.97 | 40.28 | **37.70** | **+4.65%** |
| 09 | +NAIT(CodeX) | 47.51 | 23.46 | 15.53 | 38.67 | 28.49 | 49.74 | 44.19 | 46.27 | 39.72 | 37.06 | +2.88% |
| 10 | +NAIT(TyDiQA) | 46.17 | 22.82 | 13.80 | 37.67 | 25.02 | 47.88 | 47.78 | 49.23 | 40.00 | 36.71 | +1.89% |
| 11 | +NAIT(BBH) | 47.78 | 23.36 | 13.34 | 36.67 | 25.15 | 47.08 | 45.93 | 48.46 | 40.46 | 36.47 | +1.23% |
| 12 | +NAIT(全部 7–11 綜合) | 46.83 | 23.29 | 16.53 | 39.67 | 26.44 | 47.62 | 46.09 | 48.27 | 40.02 | 37.20 | +3.24% |

**重點解讀**
- **10% 勝 100%**：NAIT（綜合，#12）AVG 37.20，較全量 Alpaca-GPT4（#01, 36.03）**+3.24%**；最強單能力 **NAIT(GSM) +4.65%**。
- **勝過既有選擇法**：AlpaGasus −2.34%、Q2Q −0.98%、Random −0.94% 都比全量還差；**SelectIT(+3.15%) 具競爭力**但仍略遜 NAIT。
- **能力會互相影響**：只用多語特徵(#10) 會拖累數學/程式；綜合(#12) 把數學從 14.63→16.53。
- **針對單能力更穩**：相對 Random，數學 **+7.53%**、多語 **+6.29%**、程式 **+5.33%**。
- **跨能力轉移**：MMLU 特徵能提升多語；CodeX 特徵能提升 GSM。

---

## 10. 消融研究與洞見（原文 §5，本論文精華）

### 10.1 選擇比例：最佳是 **top-30%**，越多越糟（Figure 2）
- 把比例從 10%→100% 掃描，**top-30% 表現最佳**；之後**整體下降，100% 時最低**。
- **洞見**：冗餘資料會損害泛化 → 必須做資料選擇。（Table 2 用 10% 是為與 baselines 公平比較；最佳甜蜜點是 30%。）

### 10.2 In-domain 樣本數量：少量即勝 Random（Figure 3，在 30% IT 下）
- 測 16/64/256/1024/… 直到各任務全量。**即使只有 16/64/256 筆，多數情況已勝過 Random**。
- 增大邊際遞減；多數任務在最大規模達峰，但 **GSM、TyDiQA 在 4096** 才達峰（18.35 / 47.57）。
- **洞見**：少量範例就能抓到能力特徵（低成本、可重用）；但 in-domain 資料**品質**也重要。

### 10.3 對齊分數方向性：高分大幅有效、低分災難性（Table 3）
| 設定 | Overall AVG | ∆ vs Random |
|---|---|---|
| Random（隨機 10%） | 34.04 | — |
| **High（對齊分數最高 10%）** | 35.18 | **+3.35%** |
| **Low（對齊分數最低 10%）** | 28.27 | **−17.54%** |
- **洞見**：對齊分數與能力增益**強正相關**——選高分有效（+3.35%），選低分**嚴重有害**（−17.54%），證明分數方向是真實的因果訊號，而非雜訊。

### 10.4 跨基礎模型皆有效（Table 4；NAIT 10% vs Alpaca 全量）
| 模型 | Alpaca(全量) | +Random(10%) | **+NAIT(10%)** |
|---|---|---|---|
| LLaMA-2-13B | 39.74 | 40.76 (+2.57%) | **42.53 (+7.02%)** |
| Mistral-7B | 33.98 | 37.69 (+10.92%) | **41.43 (+21.92%)** |
| LLaMA-3-8B | 48.57 | 52.10 (+7.27%) | **57.63 (+18.65%)** |
| Qwen-2.5-7B | 72.42 | 73.39 (+1.34%) | **75.20 (+3.83%)** |
- **洞見**：跨尺度（13B）、跨架構（Mistral/LLaMA-3）、甚至對很強的 Qwen-2.5-7B 都再提升，且一致**勝過 Random**。

### 10.5 跨 IT 資料集：最佳比例隨「資料密度」上升（Figure 4）
- **Alpaca-GPT4 ≈ 30%**、**Orca-GPT4 ≈ 50%**、**Evol-Instruct ≈ 80%** 達最佳（皆勝全量）。
- **洞見**：資料**品質/複雜度/資訊密度越高，需保留越多**（Evol-Instruct 最需要，達 80%）；冗餘越多者可砍越狠。

### 10.6 可轉移性：邏輯/程式特徵最通用（Figure 5–6，Eq. 7）
- 轉移性定義：`Transferabilityᵢ = Acc(i, j) − Acc(j, j)`（用特徵 `i` 活化、在任務 `j` 的表現，減去 `j` 用自身特徵的基線）。
- **GSM、CodeX（邏輯推理＋程式）→ 明顯正向跨任務轉移**，通用力強。
- **TyDiQA（多語）→ 轉移弱甚至負向**，依賴語言特定模式、跨域適應差。
- **綜合（aggregate 多能力成一個 comprehensive 方向）→ 整體轉移性最佳**；t-SNE 顯示各能力方向形成可分群集，綜合方向揭示「通用核心能力」。
- **洞見**：存在**跨任務穩定的通用核心特徵**；要練通用能力，**優先挑富含推理/程式結構的資料**最划算。

### 10.7 穩定核心子集（Figure 7）
- 不同能力選出的 10% 子集，**58.87% 互相重疊** → NAIT 一致地找到一個**通用核心資料集**。
- **GSM8K 最需任務特異資料**（1,034 筆獨有）；MMLU、CodeX 更依賴共享核心。
- 質性上，NAIT 偏好**需要複雜任務遵循與邏輯推理**的樣本（不像 embedding 法只看表面語意相似）。
- **洞見**：模型基礎能力由一小撮**反覆被活化的核心資料**支撐；NAIT 能在「通用核心」與「任務特異」間取得平衡。

### 10.8 不同選擇策略對照（Appendix G, Table 10；10% 資料）
| 策略 | Overall AVG | ∆ |
|---|---|---|
| Alpaca-GPT4 基線 | 34.16 | — |
| +Hard (PPL) | 24.98 | −26.87% |
| +Easy (PPL) | 32.47 | −5.53% |
| +Hard (Length) | 32.85 | −3.83% |
| +Easy (Length) | 21.17 | −38.03% |
| **+NAIT** | **35.18** | **+3.00%** |
- **洞見**：以困惑度/長度當難易度的簡單策略**大多嚴重掉分**；NAIT 是少數**正向**者。

### 10.9 效率：免外部模型、免梯度（Table 5；A800 80G, batch 8, Alpaca-GPT4 52k）
| 方法 | 不依賴外部 | 時間 | 成本 |
|---|:--:|---|---|
| AlpaGasus | ✗ | 19.07h | $178.02 |
| Q2Q | ✗ | 3.52h | $4.05 |
| LESS | ✓ | 9.86h | $11.33 |
| SelectIT | ✓ | 23.20h | $26.68 |
| **NAIT** | **✓** | **1.32h** | **$1.52** |
- **洞見**：相對 AlpaGasus/Q2Q 達 **19× / 4× 成本下降**、快 17.75h / 2.2h；相對 SelectIT **成本降 94.3%、加速 17.58×**。在準確度與成本間取得極佳平衡，可規模化。

### 10.10 vs 梯度法 LESS（Appendix E）
- LESS 在**目標任務**可較高（MMLU 48.12 > NAIT 47.81），但**過擬合目標驗證集** → **整體平均較低（39.94 < NAIT 41.31）**，非目標任務退化。
- **洞見**：NAIT 在「目標 + 非目標」都穩，**轉移性更好**；且免梯度/Hessian，計算可行。

### 10.11 跨語言延伸（Appendix H, Table 11；ALMA-7B 機器翻譯，NAIT 取 top-30%）
- En⇄De、Zh⇄En 四方向；**NAIT 整體 COMET 82.94 / BLEU 29.19**，多數指標勝過 Random(30%) 與 Full。
- **洞見**：NAIT 的資料選擇思路可延伸到**翻譯/跨語言**場景。

---

## 11. 主要貢獻（原文）
1. 提出 **NAIT——首個以「神經元激活模式」為基礎的指令資料選擇框架**，為「能力導向」的模型培養提出新範式。
2. 揭示「**激活特徵 ↔ 基礎能力保留**」的關聯，並系統性探討其**跨任務可轉移性**。
3. **開源**跨任務神經元特徵庫與 **Alpaca-NAIT** 高品質資料集。

---

## 12. 優點 / 限制 / 適用情境

**優點**
- 💰 便宜（純前向、可快取）；🔁 特徵可重用/可轉移；🎯 能力導向（可針對特定或通用能力）；🧩 跨模型/資料集/能力域穩健；🔍 可解釋（直接看內部激活、可做轉移性與 t-SNE 分析）。

**限制 / 注意**
- 仍需對候選池**逐筆前向傳播**抽激活（雖便宜，超大池仍有成本；可快取緩解）。
- 需要**少量 in-domain 參考**界定目標能力。
- 「神經元」對應**各解碼層激活**；非 GLU/SwiGLU 架構或要對齊 FFN 中介神經元時，需調整抽取點。
- 多語（TyDiQA）特徵的**跨域轉移較弱**。

**何時用 NAIT**
- 你有**大型且冗餘**的 IT 池想砍成本又保住/提升能力；想**針對特定能力**選資料；不想/不能依賴外部大模型評分或昂貴梯度法。

---

## 13. 重點速記（Cheat Sheet）
- **本質**：資料選擇 → 神經元**激活方向對齊**問題。
- **四步**：①in-domain 取 last−first token 激活位移 → ②每層 PCA 第一主成分 + 正負號校正＝方向 `vₗ` → ③候選激活投影 `sy=Σₗ A⁽ˡ⁾·vₗ` → ④取 top-k 微調。
- **不需**：外部大模型、梯度、Hessian。
- **數字**：LLaMA-2-7B + 10% Alpaca-GPT4 → AVG **+3.24%**（vs 全量），單能力 GSM **+4.65%**；High vs Low 對齊 = **+3.35% vs −17.54%**；成本 **1.32h / $1.52**。
- **比例甜蜜點**：Alpaca-GPT4 ≈ **30%**、Orca-GPT4 ≈ 50%、Evol-Instruct ≈ 80%（10% 只是對比設定）。
- **最通用資料**：富含**邏輯推理 / 程式**特徵者；存在 **58.87% 重疊的穩定核心子集**。
- **層**：用**所有解碼層**（非單層）。

---

## 14. 參考連結
- 論文（abs / HTML / PDF）：https://arxiv.org/abs/2603.13201 ｜ https://arxiv.org/html/2603.13201v1 ｜ https://arxiv.org/pdf/2603.13201
- OpenReview：https://openreview.net/forum?id=uq6UWRgzMr
- 相關工作 MoNA（同脈絡，NeurIPS 2025，用稀疏自編碼器取單義激活）：https://arxiv.org/abs/2503.15573
- 對照法：LESS、AlpaGasus、SelectIT、Q2Q（"From Quantity to Quality"）、LIMA、Instruction Mining、InsTag

---

*整理依據：作者提供之 v1 PDF 原文，逐節擷取方法公式（Eq.1–7）、Algorithm 1、Table 1–11 與各圖結論。本文件取代先前以網頁搜尋重建的版本，已修正「層選擇、特徵運算子、最佳比例」等處。*
